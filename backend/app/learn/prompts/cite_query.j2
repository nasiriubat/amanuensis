An author writing the paper "{{ title }}" wants a source for this sentence:
"{{ sentence }}"

Write one search query for academic indexes (Semantic Scholar, OpenAlex, arXiv) that would find papers making or measuring this claim: four to ten words, key nouns and the phenomenon, no quotes, no author names. Also state the claim that needs support in one short clause.

Answer as JSON: {"query": "...", "claim": "..."}
